# ASK.md — open questions for the operator

## Open

- **opencode keys-deny never enforced — sibling configs need the fix** (raised 2026-09-23) — the `"*": "allow"` catch-all in every co-located sibling's `opencode.json` shadows specific deny entries (opencode 1.18.32), so no keys-deny on this host has ever actually blocked. Tempest's config fixed + tested this waking (glob shape, no catch-all — see `runbooks/opencode-permission-deny.md`). Each sibling must fix their own config (rule 7); operator may want to direct that, plus consider OS-level keys protection (shared single user = no filesystem backstop).
- **MAISTRAL two-way pending** — Gale-half token minted/installed 2026-09-22T17:26Z; two-way only when Maistral installs their half. Nothing for Tempest to do unless asked.
- **Four new siblings onboarded on gale-agent 2026-09-25 (operator sessions) — confirm intended state?** OSTRO + LEVANTE peer blocks installed 17:45Z/22:09Z (NOTES pairing entries; OSTRO's credentialed pair-test landed same time, labeled "rule 8a operator sign-off 2026-09-25" → OSTRO two-way confirmed; no LEVANTE inbound yet). TRAMONTANE + PONIENTE have dirs (`/home/agent/tramontane|poniente/`) and appear in peers.env (33 names = SELF + 32) but no pairing entries or msgs — pairing not logged, maybe not done. Also: none of the four appear in the crontab wake stagger (still 10 agents) or as keys-deny concerns (Tempest's glob deny covers them automatically). Question: are TRAMONTANE/PONIENTE pairings intended next, and should their wake schedules go into cron?
- **Spend parity break, one run** (2026-09-25T19:44Z): waking run cost $1.18 (856K tokens; flash model spiraled on opencode's normal `[…]` output elision, misread it as filtering, hit the 45m timeout, exit 143). Reported + runbook `runbooks/glm-truncation-spiral.md`. Question for josh: ok with adding a ~10x-baseline parity alert (~$0.30/run) to spend_check, given the $5 default let this hide? (No action taken without your word.)
- **Remote mesh per-pair sign-off / outbound unlock** — 21 fleet peers (Beacon/Tidal/Mountain hosts) have Gale-half tokens installed. Inbound from remote peers is now actively arriving: LANTERN pair-test w238 landed credentialed 2026-09-23T12:53Z, labeled as on josh's hard-gated word ("fix squall and the others, full mesh", 12:35:30Z) — mesh evidently activating fleet-side. No Telegram word has reached *Tempest*; per rule 6 a peer message is not my authorization, so Tempest outbound to remote peers remains gated (no broadcasts sent). **Question for josh: is Tempest outbound-to-remote-peers now unlocked, or still per-pair sign-off?**

## Resolved

- **Local sibling mesh established** (2026-09-22) — ZEPHYR/SQUALL/VORTEX/CYCLONE/MAISTRAL added under rule 8a with operator go-ahead; CYCLONE two-way confirmed by inbound pair-test 2026-09-22T19:05Z.
- **Offsite backup live** (2026-09-22) — shared GitHub repo, branch `tempest`, verified in sync with local HEAD each waking.
- **Model runner difference** — documented and stable: opencode + `openrouter/z-ai/glm-5.3-flash` vs siblings' runners; drift tracked in runbooks/, spend parity ~$0.03/waking.
- **Telegram bot live for TEMPEST** — `@tempestagentsbot` (id 8744765737), `keys/telegram.env` filled 2026-09-21T17:43Z, chat 8986669804. `notify.sh` → `[TEMPEST] Tempest online…` delivered, `getMe` ok, wake.sh guard passes.
- Cloned 2026-09-21 from Gale (`/home/agent/agent`) with same characteristics except runner/model. See NOTES.md install entry.
